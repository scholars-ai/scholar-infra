# 复制为 langfuse.env（VPS 部署工作区内，绝不入库）
DATABASE_URL=postgres://scholar:replace-with-postgres-password@postgres:5432/langfuse?sslmode=disable
NEXTAUTH_SECRET=   # openssl rand -base64 32
SALT=              # openssl rand -base64 32
NEXTAUTH_URL=https://langfuse.example.com
